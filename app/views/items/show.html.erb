<div class="col-md-6 col-md-offset-3"
<h2><%= @item.title %></h2>
<p><%= @item.description %></p>
<% @item.images.each do |x| %>
  <%= image_tag x.image %>
<% end %>
<%= link_to "Back", root_path %>
<%= link_to "Edit", edit_item_path(@item) %>
</div>
