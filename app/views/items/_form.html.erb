<% if current_page?(action: 'index') %>
  <%= form_for @item, remote: true do |form| %>
    <%= form.label :title %>
    <%= form.text_field :title %>
    <%= form.label :description  %>
    <%= form.text_field :description  %>
    <%= form.submit :submit %>
  <% end %>
<% else %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :description  %>
  <%= form.text_field :description  %>
  <%= form.submit :submit %>
<% end %>

//form first when it's index and the other when it's edit because i don't have when i do ajex 
